import { Prisma } from "@prisma/client"; import { requireAdmin } from "@/lib/auth"; import { db } from "@/lib/db"; import { handleApiError, ok } from "@/lib/http";
export const dynamic = "force-dynamic";
export async function GET(request: Request) { try { await requireAdmin(); const q = new URL(request.url).searchParams; const page = Math.max(1, Number(q.get("page") || 1)); const pageSize = 24; const where: Prisma.PhotoWhereInput = { ...(q.get("vendor") ? { vendorId: q.get("vendor")! } : {}), ...(q.get("user") ? { userId: q.get("user")! } : {}), ...(q.get("flagged") === "true" ? { isInappropriate: true } : {}) }; const [items, total] = await Promise.all([db.photo.findMany({ where, skip: (page - 1) * pageSize, take: pageSize, orderBy: { createdAt: "desc" }, include: { vendor: { select: { id: true, name: true } }, user: { select: { id: true, name: true } } } }), db.photo.count({ where })]); return ok({ items, total, page, pageSize, pages: Math.ceil(total / pageSize) }); } catch (error) { return handleApiError(error); } }
