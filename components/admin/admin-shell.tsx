"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { BarChart3, Flag, Image, MessageSquare, Settings, Store, Users, ExternalLink } from "lucide-react"; import { clsx } from "clsx";
const nav = [{ href: "/admin", label: "Dashboard", icon: BarChart3 }, { href: "/admin/vendors", label: "Vendors", icon: Store }, { href: "/admin/reviews", label: "Reviews", icon: MessageSquare }, { href: "/admin/photos", label: "Photos", icon: Image }, { href: "/admin/reports", label: "Reports", icon: Flag }, { href: "/admin/users", label: "Users", icon: Users }, { href: "/admin/settings", label: "Settings", icon: Settings }];
export function AdminShell({ children, user }: { children: React.ReactNode; user: { name: string | null; role: string } }) { const path = usePathname(); return <div className="container-page py-6"><div className="mb-6 flex items-center justify-between rounded-2xl bg-ink px-5 py-4 text-white"><div><p className="font-black">Find The Mama Admin</p><p className="text-xs text-orange-100">{user.name || "Team member"} · {user.role}</p></div><Link className="flex items-center gap-2 text-sm font-bold" href="/">View site <ExternalLink size={16}/></Link></div><div className="grid gap-6 lg:grid-cols-[220px_minmax(0,1fr)]"><aside className="card h-fit p-2 lg:sticky lg:top-24">{nav.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={clsx("flex items-center gap-3 rounded-2xl px-4 py-3 font-bold", path === href || (href !== "/admin" && path.startsWith(href)) ? "bg-saffron-600 text-white" : "hover:bg-orange-50")}><Icon size={18}/>{label}</Link>)}</aside><main className="min-w-0">{children}</main></div></div>; }
